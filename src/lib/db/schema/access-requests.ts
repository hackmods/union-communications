import { index, jsonb, pgTable, text, timestamp, uniqueIndex } from "drizzle-orm/pg-core";
import type { AccessRequestOffering, AccessRequestStatus, AccessRequestReviewEvent } from "@/types/access-request";
export const accessRequests = pgTable("access_requests", {
  id: text("id").primaryKey(), submissionKey: text("submission_key").notNull(), kind: text("kind").notNull().$type<"local_interest" | "member_access">(),
  name: text("name").notNull(), email: text("email").notNull(), unionName: text("union_name").notNull(), localName: text("local_name").notNull(), role: text("role"), offerings: jsonb("offerings").notNull().$type<AccessRequestOffering[]>(), message: text("message"), locale: text("locale").notNull().$type<"en" | "fr">(), consentAcceptedAt: timestamp("consent_accepted_at", { withTimezone: true }).notNull(), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), status: text("status").notNull().$type<AccessRequestStatus>().default("new"), unionId: text("union_id"), localId: text("local_id"), privateNote: text("private_note"), reviewedById: text("reviewed_by_id"), inviteId: text("invite_id"), receiptSentAt: timestamp("receipt_sent_at", { withTimezone: true }), notifySentAt: timestamp("notify_sent_at", { withTimezone: true }), notificationError: text("notification_error"), reviewHistory: jsonb("review_history").notNull().$type<AccessRequestReviewEvent[]>().default([]),
}, (t) => [uniqueIndex("access_requests_submission_key_idx").on(t.submissionKey), index("access_requests_status_created_idx").on(t.status, t.createdAt), index("access_requests_local_scope_idx").on(t.unionId, t.localId, t.kind, t.status), index("access_requests_invite_idx").on(t.inviteId)]);

