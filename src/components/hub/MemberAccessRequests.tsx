"use client";
import { useEffect, useState } from "react";
import { Card } from "@/components/ui/Card";
import { Button } from "@/components/ui/Button";
type Row={id:string;name:string;email:string;unionName:string;localName:string;message?:string;status:string;createdAt:string};
export function MemberAccessRequests(){const [rows,setRows]=useState<Row[]>([]);const [error,setError]=useState("");useEffect(()=>{void fetch("/api/access-requests").then(async r=>{if(!r.ok)throw new Error();setRows((await r.json()).items)}).catch(()=>setError("Unable to load member access requests."))},[]);async function update(id:string,status:string){const r=await fetch(`/api/access-requests/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status})});if(r.ok)setRows(v=>v.map(row=>row.id===id?{...row,status}:row))}if(error)return null;return <section className="mt-8"><h2 className="text-xl font-bold text-opseu-dark">Member access requests</h2><p className="mt-1 text-sm text-gray-700">Review requests routed to this local before inviting members.</p>{rows.length?<ul className="mt-4 space-y-3">{rows.map(row=><li key={row.id}><Card density="compact"><p className="font-semibold text-opseu-dark">{row.name} · {row.status}</p><p className="text-sm text-gray-700">{row.email} · {row.unionName} · {row.localName}</p>{row.message?<p className="mt-2 text-sm text-gray-700">{row.message}</p>:null}<div className="mt-3 flex flex-wrap gap-2"><Button size="sm" onClick={()=>void update(row.id,"approved")}>Approve</Button><Button size="sm" variant="outline" onClick={()=>void update(row.id,"declined")}>Decline</Button></div></Card></li>)}</ul>:<p className="mt-3 text-sm text-gray-600">No member access requests.</p>}</section>}
