---
description: Shared Brand Kit canvas tokens + primitives for Comms export tools
globs: src/app/**/tools/**/*.tsx,src/components/tools/canvas/**/*.tsx,src/components/tools/graphic-layouts.tsx,src/components/brand/BrandKitCanvasPanel.tsx,src/lib/utils/canvas-*.ts,src/app/**/brand-kit/**/*.tsx,src/lib/comms/canvas-fonts.ts,src/app/canvas-fonts.ts
alwaysApply: false
---

# Comms visual system

Companion to [`comms-module.mdc`](comms-module.mdc). Spec: [`docs/modules/COMMS_VISUAL_SYSTEM.md`](../../docs/modules/COMMS_VISUAL_SYSTEM.md).

## Hard rules

1. **Read tokens** via `resolveCanvasTokens(brandKit)` from `useBrandStore` — do not hardcode a new full-bleed centered primary column when `CanvasTypeBlock` / `CanvasQrPlate` / `CanvasBrandHeader` fit.
2. **Style packages set tokens only** (`solid` / `field` / `workshop`). Tool layout IDs (meeting rails, solidarity stack/split, graphic notice/results) stay per-tool.
3. **Legacy default:** omit `BrandKit.canvas` → resolver matches classic layout chrome (center / flat / white-card QR) with Brand Kit typeface defaults (Montserrat + Source Sans). Do not surprise density/alignment of existing kits.
4. **Capture-safe:** inline hex/rgba inside export roots; `CanvasGrainOverlay` uses PNG tile + soft-light (never SVG `feTurbulence` alone). Duotone photos via `CanvasDuotonePhoto` (grayscale + multiply/screen).
5. **QR modules:** black on white only. Tokenize plate chrome (`qrPlate`), not module colours.
6. **Multi-union:** no OPSEU hex/names in new primitives — Brand Kit colours only.
7. **Ink:** `pickContrastingInk` / `BrandLogo backgroundColor` as in `comms-module.mdc`.
8. **Typefaces:** Brand Kit `headlineFontId` / `bodyFontId` via [`canvas-fonts.ts`](../../src/lib/comms/canvas-fonts.ts); self-hosted OFL only (`next/font/local`). Apply `tokens.headlineFontFamily` / `bodyFontFamily` on capture type. Flyer may override with `inherit` or a catalog id.
9. Update the migration register in `COMMS_VISUAL_SYSTEM.md` when a family ships.

## Do not

- Invent one-off white `#FFFFFF` QR plates when `CanvasQrPlate` exists
- Load webfonts from a CDN (ADR-014 — chrome stays system; canvas faces via `next/font/local` only)
- Invent a second geometry engine beside [`canvas-core`](canvas-core.mdc) / `CanvasWrapper`
- Brand-tint QR modules without an explicit product ticket
- Hardcode Arial / Impact on export roots when Brand Kit tokens exist
- Put `maxWidth: 100%` on `[data-export-root]` with fixed height (aspect desync)
- Put `rem` / Tailwind `text-*` on HD or print `[data-export-root]` sheets (preview scale does not fix export)
- Hard-clamp wallet body (`-webkit-line-clamp: 2`) while unused vertical slack remains above the QR

## Layout-class type (2026-09-23)

One fit strategy per class — see `COMMS_VISUAL_SYSTEM.md` “Layout-class type contract”.

| Class | Use |
|-------|-----|
| Print stack | `CanvasTypeBlock fit` + `CanvasStackSlot` |
| HD / social | design-width chrome + `CanvasFitStackedHeadline` |
| Dense QR grid | `qrBoardChrome` tabulated px |
| Wallet | `wallet*` + `WalletCopyBlock` (pack → fit → clamp last) |
| Fluid strip | `clampTypeRem` only (Board Banner) |

Wallet: copy column `flex-1 min-h-0`; QR `shrink-0`; never `mt-auto` empty band + 2-line clamp. Floors: `WALLET_TITLE_MIN_PX` / `WALLET_BODY_MIN_PX` / `WALLET_META_MIN_PX`.

## Canvas layout geometry (2026-08-17)

Session: [`session-knowledge-2026-08-17-qr-canvas-layout.md`](../../docs/audit/session-knowledge-2026-08-17-qr-canvas-layout.md). Tests: `e2e/helpers/canvas-layout.ts`, `layout-class-matrix.ts`.

1. **Multi-QR / dense posters:** cap Brand Kit display `typeScale` in chrome helpers (`qrBoardChrome`). Do not apply Flyer/Graphic display titles to a letter grid.
2. **Square QR plates:** explicit square box (`width` + `aspect-ratio: 1` or equal px wrapper). Never the same `%` on width and height. Never `max-h-full` + `object-contain` on the img inside a non-square plate — `CanvasQrPlate` must not set `maxHeight: 100%`.
3. **Reference vs link wallet cards:** explicit px square wrapper **only** for reference presets (`referencePlatePx`). Link presets render `CanvasQrPlate` directly — a shared `%`-width wrapper breaks flex shrink.
4. **Preview vs export:** scale the sheet with `transform` on a **parent** of `[data-export-root]` (`CanvasWrapper` in `src/components/canvas-core/`). `capture.ts` strips transform on the clone. Do not shrink `maxWidth: 100%` and keep design-px type without scaling. Wallet Letter (QR Card / Action Card) uses the same wrapper; keep `wallet*` helpers on design `previewWidthPx`.
5. **`includeBranding` after onboarding:** on-purpose (`themeEstablished`). Keep compact (header / mark logo on dense layouts; one truncated local-label line on wallet Letter). No second full-width footer band on multi-QR canvases.
6. **Layout overlap tests:** clip `getBoundingClientRect()` URL boxes to `[data-qr-cell]` before overlap checks; `overflow: hidden` is not enough. Keep Playwright `evaluate()` math in sync with `canvas-layout-geometry.ts`.
7. **Playwright QR waits:** poll `offsetWidth` / `naturalWidth`, not visual `getBoundingClientRect()` — parent preview scale shrinks painted size without failed render.
8. **Prefer `CanvasWrapper` for fixed print sheets** — parent of `[data-export-root]` with optional `maxScale` so previews can grow. Do not copy `qrBoardChrome` density caps onto Flyer / Graphic / Solidarity display type.
9. **Square wallet sheets:** explicit design height on export root + copy column `min-h-0 overflow-hidden`; never center the whole flex stack on square link cards (B13). Use `qrCardPreviewHeightPx`. Prefer `WalletCopyBlock` over hard description line-clamp.
10. **Wallet dead space:** if body is truncated and slack remains above the QR, fix pack/fit — do not enlarge empty flex gaps.
