---
description: Hybrid LEC RSVP outreach — shared invite email on public Comms tools; not Hub
globs: src/lib/comms/event-email*.ts,src/components/tools/InviteEmailPanel.tsx,src/app/**/tools/document-generator/**,src/app/**/tools/board-notice/**,src/app/**/tools/graphic-maker/**
alwaysApply: false
---

# Event RSVP outreach (public Comms)

## Product insights (locked)

- **Real LEC job:** hybrid meetings (on site + remote). Officers need **two numbers**: quorum = Yes (either mode); **food heads** = on-site Yes + guests only.
- **Calendar Accept/Decline cannot do this** — native invite RSVP has no on-site vs remote. Use copy-only email + Excel (R0) or token form (R1).
- **Sample invite email = public tool**, not Hub. Announcement class, no member PII, copy-only / `mailto:`. Grievance drafts stay in Hub (MFA + audit).
- **No auto-send** until ARCHITECTURE v2+ transactional email. Footer on templates must say review-before-send / no collection.

## Shipped

| Asset | Path |
|-------|------|
| Invite builder | `src/lib/comms/event-email.ts` (`buildEventInviteEmail`, `buildMailto`) |
| RSVP Excel + tallies | `src/lib/export/office-export.ts` (`renderEventRsvpXlsx`) |
| Event ICS | `src/lib/calendar/event-ics.ts` |
| UI today | Document Generator Event notice only |

## Planned next (R0.5)

Build plan: [`.cursor/plans/hybrid_lec_rsvp_outreach.plan.md`](../plans/hybrid_lec_rsvp_outreach.plan.md)

1. Extract shared **`InviteEmailPanel`** from Document Generator
2. Mount on **Board Notice** when `noticeType` is `meeting` (optionally `event`)
3. Map `headline→title`, `date`, `time`, `location`, `contact→contactName`; optional `quorumNeeded`
4. Optional Graphic Maker `meetingNotice` — only if fields exist
5. Do **not** duplicate Excel/ICS on Board Notice — link to Document Generator Event notice for the full pack

## Do

- Reuse `event-email.ts` — one source of EN/FR reply instructions (Attending + On site/Remote + dietary)
- Keep invite email **adjacent** to print export (card/step), not forced onto the poster pixels
- Unit-test any field mapper (`fieldsFromBoardNotice` or similar)
- Update EN + FR; update `docs/modules/CALENDAR_MEETINGS.md` + `docs/PROGRESS.md` when shipping R0.5

## Do Not

- Put event RSVP invite email in the Officer Hub
- Call `/api/grievances/*/email-draft` for public meetings
- Add SMTP, cron, or auto-send
- Treat Board Notice as a live RSVP collector
- Fork a second invite email template with different reply schema
