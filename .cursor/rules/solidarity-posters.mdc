---
description: Solidarity poster maker — high-energy board filler design brief
globs: src/app/**/tools/solidarity-poster/**,src/lib/constants/solidarity-slogans.ts,src/lib/constants/solidarity-poster-formats.ts,src/lib/utils/brand-theme.ts,src/lib/export/qr.ts
alwaysApply: false
---

# Solidarity Posters

High-energy union-board filler posters and device wallpapers (Keep-Calm structure, not pastiche). Route: `/tools/solidarity-poster`.

## Visual direction
- Structure: small lead-in → **huge stacked headline** → short closer
- Colours: Brand Kit primary / secondary / accent only
- Avoid: purple gradients, cream+terracotta, broadsheet/newspaper layouts
- **Print**: letter (8.5×11) and tabloid (11×17); PNG + PDF
- **Digital** (PNG only): Desktop 16:9 (3840×2160), Wide 19.5:9 (2340×1080), Phone 9:16 (1080×1920)
- Preview matches print: flat shadow card outside `canvasRef` (shadow oklch breaks capture); landscape footers use `shrink-0` + wrapping URL so QR is not clipped
- **Export-safe colours**: inside `canvasRef` use inline hex/`rgba` only — no Tailwind `border-white/30`, `text-white`, `opacity-*`, or `bg-white` (Tailwind v4 oklch/`color-mix` washes out PNGs via html-to-image)
- Footer toggles: Show CTA + URL (defaults on; falls back to unionops.org), Show QR (defaults on; tip suggests off for wallpapers)
- No member photos → no ConsentModal
- No union names in slogans or core UI — Brand Kit / seed only

## Layouts
1. **Stack** — full-bleed primary, centered type; BrandLogo + local label when branding on
2. **Split** — colour block + type; colours only (no lockup) so type stays dominant
3. **Banner** — thick accent bar + oversized word + CTA strip; BrandLogo in bar when branding on

## Local branding
Theme established when: onboarding complete, OR official/custom logo set, OR local number non-empty (`isBrandThemeEstablished`).

- Established → “Include local branding” defaults ON; Stack/Banner show prominent `BrandLogo` + local label
- Not established → toggle OFF; soft link to `/onboarding`; user may force branding on
- Always seed canvas colours from Brand Kit

## Slogan tone
Generic, punchy, solidarity-first. See `SOLIDARITY_SLOGANS` in `src/lib/constants/solidarity-slogans.ts`.

Formats live in `src/lib/constants/solidarity-poster-formats.ts`.
