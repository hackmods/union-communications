# DEPRECATED — use `.env.example` instead (SEC-010).
# This file previously shipped a hardcoded AUTH_SECRET placeholder.
# Copy from `.env.example` and generate a secret with: openssl rand -base64 32
