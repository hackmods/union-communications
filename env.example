# Auth.js — generate with: openssl rand -base64 32
AUTH_SECRET=dev-secret-change-in-production
# Public app URL (required in production behind CapRover / reverse proxy)
AUTH_URL=http://localhost:3000
